language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-hugo' ]; then cd .. && eval "mv $currentfolder generator-hugo" && cd generator-hugo; fi

